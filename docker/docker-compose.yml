services:
  falkordb:
    image: falkordb/falkordb:latest
    container_name: memex-falkordb
    ports:
      - "6379:6379" # Redis/FalkorDB protocol
      - "3000:3000" # FalkorDB browser UI
    volumes:
      - falkordb_data:/data

volumes:
  falkordb_data:
